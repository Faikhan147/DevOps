Step 1. Run The Script

Launch The EC2 instance with intance type t2.medium and volume 50 GiB by using the instance-setup-script



Step 2. Login in Nexus

Login in nexus default usename is admin and to get password run docker exec -it nexus cat /nexus-data/admin.password



Step 3. Create The New Password

After login in nexus create new password as Faisal as we used in instance-setup-script settings.xml



Step 4. Instance Setup

Run the instance-setup
