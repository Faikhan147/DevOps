Step 1. Run The Script

Launch The EC2 instance by using the instance-setup-script



Step 2. Check The Containers

docker container ps



Step 3. Check The MYSQL Databse

Check As Root
docker exec -it mysql mysql -u root -p

Check As User
docker exec -it mysql mysql -u farzan -p
