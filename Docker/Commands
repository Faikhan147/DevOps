docker run --name c1 -it ubuntu:latest -> To create the container for interactive terminal image

docker run --name c1 -P -d nginx:latest -> To create the container with random port

docker run --name c1 -p 8080:80 -d nginx:latest -> To create the container with static port

docker run --name c1 -p 3306:3306 -e MYSQL_ROOT_PASSWORD=faisalkhan35@ -d mysql:5.7 -> To create the database container

docker container ls -> To list the running containers

docker container ps -a -> To list the exited and running containers

docker stop c1 -> To stop the running container

docker kill c1 -> To stop the container forcefully

docker rm -f c1 -> To delete the running container

docker stop $(docker ps -q) -> To stop the all containers

docker kill $(docker ps -q) -> To stop the all containers forcefully

docker rm -f $(docker ps -a -q) -> To delete the all exited and running containers

docker rmi image-name -> To delete the docker image

docker rmi $(docker images -q) -> To delete the all docker images

docker images -> To list the pulled images

docker pull image-name -> To pull the images

docker push image-name -> To push the images

docker exec -it c1 bash -> To go inside the container

docker run --name c1 -v /data:/var/lib/mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=faisalkhan35@ -d mysql:5.7 -> To create the database container along mounted volume

docker run --name c2 --volumes-from c1 -p 3306:3306 -e MYSQL_ROOT_PASSWORD=faisalkhan35@ -d mysql:5.7 -> To copy the data from one container to another container

docker inspect c1 -> To check the details of the container

docker run --name c2 -p 8080:80 --link c1 -d nginx:latest -> To link one container to another container

docker network create network-name -> To create the network

docker run --name c1 -p 8080:80 --network network-name -d nginx:latest -> To create the container with custom network name

docker run --name c1 --network host -d nginx:latest -> To create the container with host network

docker network connect network-name container-id/container-name

docker network ls -> To list the networks

docker network rm network-name -> To delete the network

docker service create --name service-name --replicas=3 -d nginx:latest -> To create the service with replicas

docker service update service-name --image image-name -> To update the service

docker service update service-name --rollback -> To rollback the service

docker node ls -> To check the status of the node

docker service ls -> To list the services

docker service ps service-name -> To check the status of the service over the nodes

docker service rm service-name -> To delete the service

docker service scale service-name=5 -> To increase the replicas of the service

docker service update service-name --image image-name -> -> To update the service

docker service update service-name --rollback -> To update the service

docker swarm leave -> To remove the node from swarm

docker swarm join-token worker -> To create the token for worker node

docker swarm join-token manager -> To create the token for manager node

docker node update --availability drain node-name -> To drain the node in swarm

docker node update --availability active node-name -> To active the node in swarm

docker node promote node-name -> To promote the worker node to manager

docker node demote node-name -> To demote the manager to worker node

docker push username-of-DockerHub/customize-image-name -> To push the image in Docker Hub 

docker commit c1 username-of-DockerHub/customize-image-name -> To convert the container into customize image

docker build -t image-name . -> To build the docker file

docker commit c1 image-name -> To convert container into image

docker-compose up -d -> To run the default docker compose file 

docker compose -f compose.yaml up -d -> To run the custom docker compose file 
