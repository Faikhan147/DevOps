What is Ansible ?

It is an automation tool which helps to automate and manage the multiple operating systems


Components:-

Variables:- It is used to install the packages statically
Playbook: It is used to install multiple packages
Handler: It is used to execute the package when other package is executed
Error Handler: It is used to execute the multiple versions of the package which helps to reduce the errors
